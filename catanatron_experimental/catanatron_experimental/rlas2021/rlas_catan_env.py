from catanatron.models.enums import Action, Resource, ActionType

RLAS_ACTIONS_ARRAY = [
  (ActionType.ROLL, None),
  (ActionType.)
]

def index_to_action(index, playable_actions):


def action_to_index():


def get_actions_size():
  return len(RLAS_ACTIONS_ARRAY)